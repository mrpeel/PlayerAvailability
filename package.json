{
  "name": "lcc-availability-tracker",
  "version": "1.0.0",
  "description": "Laburnum CC Availability Tracker",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "deploy": "clasp deploy",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
